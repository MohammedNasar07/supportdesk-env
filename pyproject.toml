[project]
name = "supportdesk-env"
version = "1.0.0"
description = "Support Desk Environment - FastAPI Docker Space"
requires-python = ">=3.9"

dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "python-dotenv"
]